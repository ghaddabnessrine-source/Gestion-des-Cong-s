class Utilisateur {
  id = {
    type: Number
  };

  nom = {
    type: String
  };

  prenom = {
    type: String
  };

  email = {
    type: String
  };

  mot_de_passe = {
    type: String
  };

  role = {
    type: String
  };

  date_embauche = {
    type: String
  };

  solde_conges = {
    type: Number
  };

  jours_alloues_annuellement = {
    type: Number
  };
}

module.exports = Utilisateur;