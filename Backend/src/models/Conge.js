class Conge {
  id = {
    type: Number
  };

  id_demandeur = {
    type: Number
  };

  type_conge_id = {
    type: Number
  };

  date_debut = {
    type: String
  };

  date_fin = {
    type: String
  };

  statut = {
    type: String
  };

  motif = {
    type: String
  };

  commentaire_refus = {
    type: String
  };
}

module.exports = Conge;